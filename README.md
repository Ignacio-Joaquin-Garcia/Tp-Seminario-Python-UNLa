# Tp-Seminario-Python-UNLa

## Installation

- Run "python -m venv .venv" for creating a new virtual environment
- Run "source .venv/Scripts/activate" to active the virtual environment
- Run "pip install -r requirements.txt" to install the necessary libraries
- Run "fastapi run" to run the app at a localhost with default port 8000